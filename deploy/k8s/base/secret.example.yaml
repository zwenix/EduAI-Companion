# ─────────────────────────────────────────────────────────────────────────────
# Template only — DO NOT apply as-is.
#
# Create the real Secret once, with live values, e.g.:
#
#   kubectl create secret generic eduaicompanion-secrets \
#     -n eduaicompanion \
#     --from-literal=GEMINI_API_KEY="AIza..." \
#     --from-literal=APP_URL="https://eduaicompanion.example.com" \
#     --from-literal=ALIBABA_API_KEY="sk-ws-..." \
#     --from-literal=GROQ_API_KEY="..." \
#     --from-literal=OCR_SPACE_API_KEY="..." \
#     --from-literal=ELEVENLABS_API_KEY="..." \
#     --from-literal=HUGGINGFACE_API_KEY="..." \
#     --from-literal=REPLICATE_API_TOKEN="..." \
#     --from-literal=OPENROUTER_API_KEY="..." \
#     --from-literal=NVIDIA_API_KEY="..."
#
# Keys mirror .env.example. APP_URL must be the public URL users hit
# (Ingress host). The Deployment references this Secret with optional: true,
# so missing keys degrade gracefully instead of breaking pod startup.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: eduaicompanion-secrets
  namespace: eduaicompanion
type: Opaque
stringData:
  GEMINI_API_KEY: "REPLACE_ME"
  APP_URL: "https://REPLACE_ME.example.com"
  GROQ_API_KEY: ""
  ALIBABA_API_KEY: "REPLACE_ME"
  OCR_SPACE_API_KEY: ""
  ELEVENLABS_API_KEY: ""
  HUGGINGFACE_API_KEY: ""
  REPLICATE_API_TOKEN: ""
  OPENROUTER_API_KEY: ""
  NVIDIA_API_KEY: ""
